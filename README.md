# Библиотека AI Агентов для Backend Разработки

Это унифицированная библиотека правил (Rules), сценариев (Workflows) и навыков (Skills) для автоматизации backend-разработки с помощью AI-агентов.

## 🧠 Философия: Унификация и Абстракция

Библиотека построена на трех принципах:

1.  **Rules (Правила) — Абстрактны:** Они описывают _требуемое состояние_ (например, "тесты должны проходить"), а не конкретные команды (не "make test"). Это делает правила применимыми к любому стеку (Python, JS, Go).
2.  **Workflows (Сценарии) — Технологически-агностичны:** Сценарии описывают _процесс_ (Планирование -> Реализация -> Тесты), а конкретный стек технологий выбирается динамически.
3.  **Skills (Навыки) — Специализированы:** Навыки знают, как работать с конкретными инструментами (например, `backend-developer` знает FastAPI и SQLAlchemy).

---

## 🚀 Как использовать (Пошагово)

Чтобы запустить агента для создания или доработки проекта:

### Шаг 1: Выберите Workflow

Определите, какую задачу вы хотите решить.

- **Создание проекта с нуля:** Используйте `backend-project-full-cycle`.
- **Реализация одной фичи:** Используйте `feature-implementation-flow`.

### Шаг 2: Подготовьте Prompt

Зайдите в директорию `prompts/` и выберите шаблон, соответствующий вашему Workflow.

- Обновите поля `project_name` и `business_logic_description`.
- _(Опционально)_ Укажите `tech_stack` (если хотите форсировать технологии) или удалите этот блок, чтобы агент сам уточнил стек.

**Пример (`prompts/ru-kilocode-backend-project-full-cycle.md`):**

```yaml
workflow: backend-project-full-cycle
project_name: my-new-service
business_logic_description: "API для управления заказами..."
```

### Шаг 3: Запустите Агента

Скопируйте содержимое вашего промпта и отправьте его агенту.
Агент:

1.  Распознает Workflow.
2.  Проверит входные данные.
3.  Начнет выполнение по фазам (Planning -> Implementation -> Verification).

---

## 📊 Методология и Оценка Качества

Я оцениваю полноту библиотеки по методологии **"Backend Lifecycle Coverage"**, которая разбивает разработку на 4 ключевых этапа.

### Текущее Покрытие (Backend Software Area)

| Этап Lifecycle                 | Артефакты                                              | Оценка Покрытия      | Комментарий                                                                                            |
| :----------------------------- | :----------------------------------------------------- | :------------------- | :----------------------------------------------------------------------------------------------------- |
| **1. Planning & Architecture** | `prompt-project-planner`, `backend-architecture-rule`  | **🟢 Высокое (90%)** | Агент умеет выяснять требования, выбирать стек и проектировать слои (API/Service/Repo).                |
| **2. Implementation**          | `app-builder`, `backend-developer`, `code-style-guide` | **🟢 Высокое (85%)** | Есть специализированные скиллы для Python/FastAPI. Для других языков используется общий `app-builder`. |
| **3. Quality Assurance**       | `testing-ci-guide`, `blackbox-testing`                 | **🟢 Высокое (80%)** | Правила требуют 70%+ покрытия и E2E тестов. Workflow форсирует успешное прохождение тестов.            |
| **4. DevOps & Deployment**     | `ci-cd-deployment-guide`, `env-settings-guide`         | **🟡 Среднее (50%)** | Правила описывают требования к Docker/CI, но нет готового Workflow для _деплоя_ (только build/test).   |

### Качество Артефактов

- **Унификация:** Все артефакты имеют единый YAML-заголовок (`type`, `inputs`, `outputs`).
- **Чистота:** Правила очищены от `make`-специфичных команд и могут применяться в любом проекте.
- **Адаптивность:** Workflow `backend-project-full-cycle` теперь умеет спрашивать стек технологий, если он не задан.

---

## 📚 Справочник Артефактов

### 📜 Rules (Правила)

_Лежат в `rules/`. Задают ограничения._

- `backend-architecture-rule.md`: Слоистая архитектура (API -> Service -> Repo).
- `code-quality-guide.md`: Требования к линтингу и форматированию.
- `testing-ci-guide.md`: Требования к тестам (Unit + E2E).

### 🔄 Workflows (Сценарии)

_Лежат в `workflows/`. Управляют процессом._

- `backend-project-full-cycle.md`: Полный цикл от идеи до готового сервиса.
- `feature-implementation-flow.md`: Внедрение одной фичи в существующий код.

### 🛠️ Skills (Навыки)

_Лежат в `skills/`. Дают возможности._

- `prompt-project-planner`: Интервьюирование пользователя и составление плана.
- `backend-developer`: Написание кода на Python/FastAPI/SQLAlchemy.
- `app-builder`: Общий оркестратор сборки приложений.
