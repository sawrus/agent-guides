# software

## 🧠 Agent Philosophy

В проекте используется **Antigravity / KiloCode модель управления агентом**.

```
WORKFLOW
├─ выбирает RULES
└─ выбирает SKILLS
└─ выполняют действия, строго соблюдая RULES
```

**Ключевой принцип:**  
Prompt → запускает Workflow → Workflow управляет Skills → Rules всегда enforced.

## 📁 Структура репозитория

```text
.kilocode/
├── rules/        # Жёсткие ограничения (DO / DON'T)
├── skills/       # Компетенции и инструменты агента
├── workflows/    # Сценарии выполнения задач
└── README.md     # Краткая карта системы

.agent/
├── rules/        # Runtime-правила для агента
├── skills/
├── workflows/
└── README.md

Проектное расширение

your-project/
└── .kilocode/
    ├── skills/           # Общие skills проекта
    └── skills-code/      # Skills для code-mode
```

- .kilocode/ и .agent/ — не документация для людей
- это машиночитаемый контракт поведения агента.

## 🤖 Ollama Models

### Рекомендуемая конфигурация:

- Coding → qwen3-coder:30b
- Wording / Docs → gemma3:27b
    - num_ctx: 50k – 100k

## 🧾 Prompts

Prompt — это только входные параметры, не логика.

Спецификация

- Размер: 1000–2000 символов
- Без пошаговых инструкций

Примеры

- en-antigravity-backend-project-full-cycle.md
- ru-kilocode-backend-project-full-cycle.md

## 📜 Rules (rules/)

Rules — это guardrails. Нарушение rules = провал задачи.

Используются для:

- безопасности
- качества
- архитектурной консистентности

Примеры:

- запрещено писать код без type hints
- запрещено менять схему БД без миграций
- запрещено async без timeout / cancellation
- запрещено работать с prod без workflow

### Rule specification

Поддерживаемые триггеры:

- trigger: always_on
- trigger: model_decision
- trigger: manual
- trigger: glob

Пример:

```
---
trigger: always_on
glob: api-design-guide
description: API contracts must be explicit, validated, and versioned
---

# API Design Rule

...
```

## 🛠️ Skills (skills/)

Skills — это возможности агента, не обязанности.

Skills описывают:

- стек технологий
- используемые библиотеки
- архитектурные паттерны
- роли (architect, builder, tester)

Особенности:

- skills НЕ накладывают запретов
- skills НЕ управляют порядком действий
- skills выбираются workflow

Полезные источники:

- https://github.com/Kilo-Org/kilo-marketplace
- https://github.com/anthropics/skills
- https://github.com/sickn33/antigravity-awesome-skills

### Skill specification

```
---
name: bash-pro
description: Defensive Bash scripting for CI/CD and automation
---

## Use this skill when

- Writing or reviewing Bash scripts
```

## 🔄 Workflows (workflows/)

Workflow — единственный объект, который управляет процессом.

Workflow:

- выбирает rules
- выбирает skills
- определяет порядок фаз выполнения

Используется для:

- full-cycle backend разработки
- деплоя сервисов
- миграций БД
- тестирования (unit / e2e / svt)

Если workflow существует — агент обязан ему следовать.

### Workflow specification

```
---
description: Build and test Android app
---

# Build and Test Android App

...
```

## 🔁 Change Policy

- изменения в rules/ → breaking
- изменения в skills/ → безопасные
- изменения в workflows/ → уточнение процессов

Рекомендуется:

- фиксировать изменения в git history
- использовать ADR для крупных архитектурных решений

## 🧩 TL;DR

- Rules — что нельзя нарушать
- Skills — что агент умеет
- Workflows — как правильно работать
- Prompt — только входные параметры

## 🛠 apply_for_specific_project.sh

```
bash apply_for_specific_project.sh ~/projects/ai/qwen/python_test_project
```

Скрипт копирует в директории .agent/ и .kilocode/ проекта:

- rules
- skills
- workflows
