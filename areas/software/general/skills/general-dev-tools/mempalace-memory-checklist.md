# MemPalace memory checklist

1. Confirm MemPalace MCP is configured for the active IDE agent runtime.
2. Resolve memory facts from MemPalace before answering from LLM memory.
3. Cite when memory MCP was used versus when fallback was used.
4. If memory lookup fails, continue safely and report the failure mode.
