# 🧠 Архитектура агентных систем: Rules, Skills, Workflows

Этот документ объясняет **лучшую практику (best practice)** организации агентных систем,
которую используют концептуально **Google, Anthropic и KiloCode**.

Цель архитектуры — **предсказуемость, управляемость и масштабируемость агентов**.

---

## 🧩 Базовые сущности

Агентная система всегда строится из **трёх разных уровней**:

- **Rules (Правила)** — ограничения и законы
- **Skills (Навыки)** — исполняемые способности
- **Workflows (Сценарии)** — порядок действий

❗ Эти уровни **не равноправны** и **не взаимозаменяемы**.

---

## 🏛️ Rules — Законы системы

### Что такое Rules

Rules — это **что разрешено и что запрещено**.

Они задают:

- стандарты качества
- архитектурные инварианты
- границы безопасности

Rules **НЕ описывают шаги** и **НЕ говорят, что делать**.

### Свойства Rules

- Пассивные
- Работают всегда (`always_on` или по `glob`)
- ❌ Ничего не вызывают
- ❌ Не выполняют код
- ❌ Не зависят от skills или workflows

### Примеры Rules

- Код только в `src/`
- Все модели через Pydantic
- Все миграции только через Alembic
- Каждый файл кода должен иметь unit-тест
- Запрещены прямые коммиты в `main`

> **Ментальная модель:**  
> Rules = законы физики. Их нельзя “обойти”, можно только нарушить.

---

## 🛠️ Skills — Инструменты агента

### Что такое Skills

Skills — это **то, что агент умеет делать**.

Они описывают:

- алгоритм действий
- роль агента
- порядок мышления
- допустимые инструменты

### Свойства Skills

- Активные
- Исполняемые
- Могут:
    - читать файлы
    - писать код
    - задавать вопросы
- ✅ ОБЯЗАНЫ соблюдать Rules
- ❌ Не управляют процессом целиком

### Примеры Skills

- `prompt-project-planner` — планирование проекта
- `app-builder` — разработка FastAPI
- `blackbox-test-skill` — e2e / svt тестирование
- `migration-writer` — написание Alembic миграций

> **Ментальная модель:**  
> Skills = инструменты в руках инженера.

---

## 🔄 Workflows — Оркестрация

### Что такое Workflows

Workflows описывают **когда, в каком порядке и какие skills использовать**.

Это сценарий выполнения задачи от начала до конца.

### Свойства Workflows

- Управляют процессом
- Выбирают:
    - какие skills применить
    - в каком порядке
- НЕ нарушают rules
- НЕ реализуют логику сами

### Примеры Workflows

- Feature Development Workflow
- Bugfix Workflow
- Release Workflow
- CI / CD Workflow

Пример:

1. Планирование → `prompt-project-planner`
2. Разработка → `app-builder`
3. Unit-тесты → `testing-ci-guide`
4. E2E → `blackbox-test-skill`
5. Git & Versioning → `git-versioning-guide`

> **Ментальная модель:**  
> Workflows = чек-лист или playbook.

---

## 🔗 Кто кого вызывает (главное!)

### ✅ Правильная иерархия

```
WORKFLOW
 ├─ выбирает RULES
 └─ выбирает SKILLS
        └─ исполняют действия, соблюдая RULES
```

### ❌ Запрещённые зависимости

- ❌ Rules не вызывают skills
- ❌ Skills не управляют workflow
- ❌ Skills не ослабляют rules
- ❌ Workflows не могут отменить rules

---

## 🧠 Простое объяснение «на пальцах»

| Роль     | Аналогия                      |
|----------|-------------------------------|
| Rules    | Законы и техника безопасности |
| Skills   | Инструменты и умения          |
| Workflow | Инструкция / порядок работы   |

**Пример:**

- Rule: «Всегда пристёгиваться»
- Skill: «Водить машину»
- Workflow: «Доехать из A в B»

---

## ✅ Best Practice (коротко)

- **Rules** — описывают *качество и границы*
- **Skills** — описывают *как делать*
- **Workflows** — описывают *когда и в каком порядке*

Если ты путаешь эти уровни — агент становится:

- непредсказуемым
- сложным для масштабирования
- опасным для автоматизации

---

## 🛸 Antigravity Principle

> Хороший агент:
> - подчиняется rules
> - действует через skills
> - управляется workflows
> - и никогда не смешивает роли